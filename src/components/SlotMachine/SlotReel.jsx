// 슬롯 릴 단일 컴포넌트 (토핑 그룹 1개 담당)
// props: items, isHeld, onHold, spinning, result

export default function SlotReel({ items, isHeld, onHold, spinning, result }) {
  return <div>SlotReel — 구현 예정</div>;
}
